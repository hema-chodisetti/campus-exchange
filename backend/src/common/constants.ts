// Report auto-hide threshold: number of reports needed to auto-hide a listing
export const AUTO_HIDE_REPORT_THRESHOLD = 5;

// Minimum account age (in ms) for a report to count toward auto-hide
export const REPORT_ACCOUNT_AGE_MS = 24 * 60 * 60 * 1000; // 24 hours

// Default pagination
export const DEFAULT_PAGE_LIMIT = 20;

// Maximum listing images allowed
export const MAX_LISTING_IMAGES = 10;
